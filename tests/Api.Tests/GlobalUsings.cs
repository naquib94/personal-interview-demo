// Usings that every step definition file in this suite needs.
//
// Declaring them once keeps the top of each step file free of eight lines of boilerplate, so
// what a reader sees first is the steps themselves. Kept deliberately short: a long list of
// global usings makes it hard to tell where a type came from, which is a worse problem than
// repeating a using directive.

global using AwesomeAssertions;
global using AwesomeAssertions.Execution;
global using Reqnroll;
